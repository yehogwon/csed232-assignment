name: ASSN3 CI

on:
  push:
    branches: [ "main" ]
    paths: 
        - assn3/**

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: cd assn3 && make
