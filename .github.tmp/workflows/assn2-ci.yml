name: ASSN2 CI

on:
  push:
    branches: [ "main" ]
    paths: 
        - assn2/**

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: cd assn2 && make
