name: build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: jurplel/install-qt-action@v3
    - uses: actions/checkout@v3
    - name: build
      run: |
        cmake -S . -B build
        cd build
        make -j
